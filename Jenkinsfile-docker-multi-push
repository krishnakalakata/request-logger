pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t krishnakalakata/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag krishnakalakata/request-logger:${env.BUILD_ID} krishnakalakata/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push krishnakalakata/request-logger:${env.BUILD_ID}"
        sh "docker push krishnakalakata/request-logger:latest"
      }
    }
  }
}
